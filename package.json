{
  "name": "chatroom",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "repository": "https://github.com/pengliheng/chatroom.git",
  "author": "plh2 <pengliheng111@gmail.com>",
  "license": "MIT",
  "scripts": {
    "db": "docker-compose -f ./db/docker-compose.yml up",
    "bootstrap": "lerna bootstrap",
    "dev": "yarn db && lerna run dev --parallel",
    "build": "lerna run build --parallel",
    "deploy": "docker-compose up"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.0.3"
  }
}
