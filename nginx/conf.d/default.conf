server {
    listen 8081;
    server_name 0.0.0.0;
    charset utf-8;
    location / {
        proxy_set_header Host $host;
        root /var/www/app;
        try_files $uri $uri/ /index.html;
        index  index.html;
    }
}

server {
    listen 8080;
    server_name 0.0.0.0;
    charset utf-8;

    location / {
        proxy_pass http://backend:8080;
        proxy_set_header Host $host;
    }
}
